---
# file: httpd/tests/tasks/main.yml
#
# tasks file
#

- include: build/main.yml
  when: httpd_installation == 'build'

- include: package/main.yml
  when: httpd_installation != 'build'

- include: modules.yml
  tags: modules

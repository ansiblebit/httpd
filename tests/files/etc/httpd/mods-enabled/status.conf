# Ansible managed file, do not edit directly

<IfModule status_module.c>
    <Location /httpd-status>
        SetHandler server-status
        Require ip 127.0.0.1/32
    </Location>

    ExtendedStatus On

    <IfModule mod_proxy.c>
        ProxyStatus On
    </IfModule>
</IfModule>
